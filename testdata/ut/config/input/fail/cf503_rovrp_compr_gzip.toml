##################################################################################################
## Rollover policy with compression, but not suppported by core library.
##
[policies.rollover.my_default]
condition = "size > 20m"
keep = 9
compression = "gzip"
