##################################################################################################
## Output format without triggers.
##
[formats]
  [[formats.output.my_format]]
  triggers = [ "message" ]
  levels = [ "all" ]
