##################################################################################################
## Fallback root directory with undefined environment variable
##
[system]
  fallback_path = "$Env[NONEXISTENT]"
